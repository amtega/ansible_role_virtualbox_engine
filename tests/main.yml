---
# Tasks for tesing role

- name: configure vagrant sandbox environment
  hosts: localhost
  roles:
    - role: amtega.vagrant_presets
  tasks:
    - set_fact:
        vagrant_sandbox_boxes: >-
          {{ vagrant_presets_boxes + extra_boxes }}
        vagrant_sandbox_vms: >-
          {{ (vagrant_presets_vms + extra_vms)
             | vagrant_presets_randomize_names }}
  vars:
    extra_boxes:
      - name: debian_9
        address: generic/debian9
    extra_vms:
      - name: debian_9
        box: debian_9
        hostname: debian-9
        ansible_become: true

- name: run idempotence test
  hosts: localhost
  roles:
    - role: amtega.vagrant_sandbox
      vagrant_sandbox_state: started
  tags:
    - sandbox

- name: test virtualbox_engine role
  hosts: vagrant_sandbox_vms
  roles:
    - role: amtega.virtualbox_engine
      virtualbox_engine_users:
        - root
  tasks:
    - name: get virtualbox metrics info
      shell: VBoxManage metrics list
      register: virtualbox_metrics_info_result
      changed_when: false

    - name: verify virtualbox metrics info
      assert:
        that: >-
          {{ virtualbox_metrics_info_result.stdout
             | search("CPU/Load/User") }}
  tags:
    - idempotence

- name: cleanup vagrant sandbox
  hosts: localhost
  roles:
    - role: amtega.vagrant_sandbox
      vagrant_sandbox_state: absent
  tags:
    - sandbox
